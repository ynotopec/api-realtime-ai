[project]
name = "api-realtime-ai"
version = "0.1.0"
description = "Realtime speech bridge configured for Voxtral via vLLM"
requires-python = ">=3.10"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "requests",
  "numpy",
  "webrtcvad",
  "websockets",
]

[tool.uv]
package = false
