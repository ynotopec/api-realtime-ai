apiVersion: v1
kind: Service
metadata:
  name: {{ include "realtime-bridge.fullname" . }}
  labels:
    {{- include "realtime-bridge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "realtime-bridge.selectorLabels" . | nindent 4 }}
